# ============================================
# 🔥 REQUIRED CREDENTIALS FOR PRODUCTION
# ============================================
# Global Sentinel REQUIRES all these credentials to work.
# No fallback mode - real APIs only!

# ============================================
# Google Cloud / Gemini AI (REQUIRED)
# ============================================
GOOGLE_CLOUD_PROJECT=your-google-cloud-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Setup Guide:
# 1. Create project: https://console.cloud.google.com
# 2. Enable Vertex AI API
# 3. Run: gcloud auth application-default login
# OR set GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json

# ============================================
# Elastic Cloud (REQUIRED)
# ============================================
ELASTIC_CLOUD_ID=your-elastic-cloud-id-here
ELASTIC_API_KEY=your-elastic-api-key-here

# Setup Guide:
# 1. Create free account: https://cloud.elastic.co/registration
# 2. Create deployment → Select "General Purpose" (1GB RAM free tier)
# 3. Copy Cloud ID from deployment overview page
# 4. Create API Key:
#    a. Go to: Management → Stack Management → Security → API Keys
#    b. Click "Create API key"
#    c. Name: "global-sentinel-api"
#    d. Set expiration: "Never" (for dev) or custom duration
#    e. IMPORTANT: Copy the ENCODED KEY (long base64 string)
#    f. Do NOT copy the "ID" field - copy the full encoded key
# 5. Paste the ENCODED key as ELASTIC_API_KEY above
#
# Note: If you get 401 errors, your API key is invalid/expired.
# Generate a new one from the Elastic Cloud console.

# ============================================
# Firebase (OPTIONAL - for data persistence)
# ============================================
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Setup Guide:
# 1. Create project: https://console.firebase.google.com
# 2. Enable Firestore Database
# 3. Download service account JSON
# 4. Copy credentials here

# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# 🎯 For Hackathon Judges
# ============================================
# The system REQUIRES real Gemini + Elastic credentials.
# See SETUP_CREDENTIALS_GUIDE.md for step-by-step setup.
# Takes ~15 minutes to configure all services.
